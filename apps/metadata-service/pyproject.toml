[project]
name = "metadata-service"
version = "0.1.0"
description = "Data-source connection metadata (non-secret), the schema catalog, and catalog sync"
readme = "README.md"
authors = [
    { name = "Akhil Krishna", email = "akhilmavannoor@gmail.com" }
]
requires-python = ">=3.12,<3.13"
dependencies = [
    "alembic>=1.20.0",
    "asyncpg>=0.31.0",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "platform-auth",
    "platform-observability",
    "platform-secrets",
    "pydantic-settings>=2.15.0",
    "pydantic>=2.13.5",
    "sqlalchemy[asyncio]>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
metadata-service = "metadata_service.main:run"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[tool.uv.sources]
platform-auth = { workspace = true }
platform-observability = { workspace = true }
platform-secrets = { workspace = true }
